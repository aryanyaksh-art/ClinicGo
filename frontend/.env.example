# Used server-side only, by /api/sweep, to write fresh scrape results.
# Get these from the ClinicGo Supabase project and Firecrawl account, then
# fill in .env.local (which is gitignored) — never commit real values here.
SUPABASE_URL=https://tswmmqhtikfrmmimncpy.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
FIRECRAWL_API_KEY=
