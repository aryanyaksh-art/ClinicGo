PORT=3000

# Firecrawl (https://firecrawl.dev) - used to scrape clinic websites
FIRECRAWL_API_KEY=

# Supabase project (backend uses the service role key - never expose to frontend)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
